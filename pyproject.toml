[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crawlers"
]

[tool.uv.sources]
crawlers = { workspace = true }

[tool.uv.workspace]
members = [
    "crawlers"
]
